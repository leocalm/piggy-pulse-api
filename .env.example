# Environment variables for Budget API
#
# Copy this to .env for local development
# These take precedence over Budget.toml

# Database (required)
DATABASE_URL=postgres://postgres:example@localhost:5432/budget_db

# Or use the prefixed format:
# BUDGET_DATABASE_URL=postgres://postgres:example@localhost:5432/budget_db
# BUDGET_DATABASE_MAX_CONNECTIONS=16
# BUDGET_DATABASE_MIN_CONNECTIONS=4
# BUDGET_DATABASE_CONNECTION_TIMEOUT=5
# BUDGET_DATABASE_ACQUIRE_TIMEOUT=5

# Server
# BUDGET_SERVER_PORT=8000
# BUDGET_SERVER_ADDRESS=127.0.0.1

# Logging
# BUDGET_LOGGING_LEVEL=info
# BUDGET_LOGGING_JSON_FORMAT=false

# Rocket (required for non-debug profiles)
# Generate with: openssl rand -base64 32
# ROCKET_SECRET_KEY=replace-with-random-base64-32-bytes
